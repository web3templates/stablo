import { Subscriber } from "rxjs";
export declare class OperatorSubscriber<TSource, TDestination> extends Subscriber<TSource> {
    constructor(destination: Subscriber<TDestination>, handlers: {
        next?: (value: TSource) => void;
        error?: (error: unknown) => void;
        complete?: () => void;
    });
}
