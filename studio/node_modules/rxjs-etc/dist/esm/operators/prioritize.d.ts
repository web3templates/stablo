import { Observable, OperatorFunction } from "rxjs";
export declare function prioritize<T, R = T>(selector: (prioritized: Observable<T>, deprioritized: Observable<T>, ...rest: Observable<T>[]) => Observable<R>): OperatorFunction<T, R>;
