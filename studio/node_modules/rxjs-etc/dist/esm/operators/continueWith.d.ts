import { ObservableInput, ObservedValueOf, OperatorFunction } from "rxjs";
export declare const continueWith: <T, O extends ObservableInput<any>>(mapper: (value: T) => O) => OperatorFunction<T, T | ObservedValueOf<O>>;
