import { MonoTypeOperatorFunction } from "rxjs";
export interface TapSubscriberConfig {
    ignore?: {
        complete?: boolean;
        error?: boolean;
    };
    subscribe?: () => void;
    unsubscribe?: () => void;
}
export declare function tapSubscribe<T>(config: TapSubscriberConfig): MonoTypeOperatorFunction<T>;
export declare function tapSubscribe<T>(subscribe: () => void): MonoTypeOperatorFunction<T>;
