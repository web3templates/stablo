import { OperatorFunction, SchedulerLike } from "rxjs";
export declare function startWithTimeout<T, S = T>(value: S, duration: number | Date, scheduler?: SchedulerLike): OperatorFunction<T, S | T>;
