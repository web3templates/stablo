import { Observable, OperatorFunction, Subject } from "rxjs";
export declare function bucketBy<T>(count: number, hashSelector: (value: T, index: number) => number, subjectSelector?: () => Subject<T>): OperatorFunction<T, Observable<T>[]>;
