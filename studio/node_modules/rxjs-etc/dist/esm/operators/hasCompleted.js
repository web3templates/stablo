import { concat, of } from "rxjs";
import { ignoreElements } from "rxjs/operators";
export function hasCompleted() {
    return (source) => concat(source.pipe(ignoreElements()), of(true));
}
//# sourceMappingURL=hasCompleted.js.map