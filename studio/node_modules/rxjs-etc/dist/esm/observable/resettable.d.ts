import { Observable, Subject } from "rxjs";
export declare function resettable<T, U extends any[]>(factory: (...args: U) => Subject<T>, ...args: U): {
    readonly closed: boolean;
    readonly observable: Observable<T>;
    readonly subject: Subject<T>;
    reset(...args: U): void;
    unsubscribe(): void;
};
