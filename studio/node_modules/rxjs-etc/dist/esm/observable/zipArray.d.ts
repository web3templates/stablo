import { Observable } from "rxjs";
export declare function zipArray<T>(observables: Observable<T>[]): Observable<T[]>;
export declare function zipArray<T, R>(observables: Observable<T>[], project: (values: T[]) => R): Observable<R>;
