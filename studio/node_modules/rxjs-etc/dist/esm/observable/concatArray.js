import { concat, EMPTY } from "rxjs";
export function concatArray(observables) {
    if (observables.length === 0) {
        return EMPTY;
    }
    return concat.apply(null, observables);
}
//# sourceMappingURL=concatArray.js.map