import { EMPTY, merge } from "rxjs";
export function mergeArray(observables, concurrent) {
    if (observables.length === 0) {
        return EMPTY;
    }
    const applyArgs = [...observables];
    if (concurrent) {
        applyArgs.push(concurrent);
    }
    return merge.apply(null, applyArgs);
}
//# sourceMappingURL=mergeArray.js.map