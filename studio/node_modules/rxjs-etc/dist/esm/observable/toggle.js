import { filter, map, share } from "rxjs/operators";
export function toggle(source, states = 2) {
    const result = [];
    const indexed = source.pipe(map((value, index) => [index, value]), share());
    for (let state = 0; state < states; ++state) {
        result.push(indexed.pipe(filter(([index]) => index % states === state), map(([, value]) => value)));
    }
    return result;
}
//# sourceMappingURL=toggle.js.map