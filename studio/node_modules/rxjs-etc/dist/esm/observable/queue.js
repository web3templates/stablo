import { queueScheduler } from "rxjs";
import { observeOn } from "rxjs/operators";
export function queue(...sources) {
    return sources.map((source) => source.pipe(observeOn(queueScheduler)));
}
//# sourceMappingURL=queue.js.map