import { Observable, OperatorFunction } from "rxjs";
export declare function switchMapUntil<T, I, R>(prelude: OperatorFunction<T, I>, project: (value: I, index: number) => Observable<R>): OperatorFunction<T, R>;
