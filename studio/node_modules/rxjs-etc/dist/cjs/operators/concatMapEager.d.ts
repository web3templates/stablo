import { ObservableInput, ObservedValueOf, OperatorFunction } from "rxjs";
export declare function concatMapEager<T, O extends ObservableInput<any>>(project: (value: T, index: number) => O, concurrency?: number): OperatorFunction<T, ObservedValueOf<O>>;
