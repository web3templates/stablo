import { Observable, Subscription } from "rxjs";
export declare class NotificationQueue extends Observable<number> {
    private _count;
    private _indices;
    private _notifications;
    constructor(notifier: Observable<any>);
    connect(): Subscription;
}
