import { Observable } from "rxjs";
import { ObservableValues } from "../util";
export interface ProgressState {
    finalized: number;
    nexted: number;
    total: number;
}
export declare function progress<Observables extends Observable<any>[], SourceSelector extends (obervables: Observables) => Observable<any>, T>(observables: Observables, resultSelector: (state: Observable<ProgressState>, source: ReturnType<SourceSelector>) => Observable<T>, sourceSelector: SourceSelector): Observable<T>;
export declare function progress<Observables extends Observable<any>[], T>(observables: Observables, resultSelector: (state: Observable<ProgressState>, source: Observable<ObservableValues<Observables>>) => Observable<T>): Observable<T>;
