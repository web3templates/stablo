import { SchedulerLike, Subscription } from "rxjs";
import { Zone } from "./Zone";
export declare class EnterZoneScheduler {
    private zone;
    private scheduler;
    constructor(zone: Zone, scheduler: SchedulerLike);
    schedule(...args: any[]): Subscription;
}
