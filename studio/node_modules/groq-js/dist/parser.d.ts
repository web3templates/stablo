import * as NodeTypes from './nodeTypes';
import { Mark, MarkProcessor, MarkVisitor } from './markProcessor';
export declare type NodeBuilder<P = NodeTypes.SyntaxNode> = (this: MarkVisitor, processor: MarkProcessor, mark: Mark) => P;
export declare type NodeBuilderArgs = [MarkProcessor, Mark];
/**
 * Parses a GROQ query and returns a tree structure.
 */
export declare function parse(input: string): any;
