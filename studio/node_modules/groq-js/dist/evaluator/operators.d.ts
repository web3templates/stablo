import { SyntaxNode } from '../nodeTypes';
import { Value } from './value';
import { Scope, Executor } from './';
declare type GroqOperator = '==' | '!=' | '>' | '>=' | '<' | '<=' | '+' | '-' | '*' | '/' | '%' | '**' | 'in' | 'match';
declare type GroqOperatorFn = (left: SyntaxNode, right: SyntaxNode, scope: Scope, execute: Executor) => Value | PromiseLike<Value>;
export declare const operators: {
    [key in GroqOperator]: GroqOperatorFn;
};
export {};
