var __create = Object.create;
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __markAsModule = (target) => __defProp(target, "__esModule", { value: true });
var __require = typeof require !== "undefined" ? require : (x) => {
  throw new Error('Dynamic require of "' + x + '" is not supported');
};
var __export = (target, all) => {
  __markAsModule(target);
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __reExport = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, { get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable });
  }
  return target;
};
var __toModule = (module2) => {
  return __reExport(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? { get: () => module2.default, enumerable: true } : { value: module2, enumerable: true })), module2);
};
__export(exports, {
  color: () => color
});
var import_color = __toModule(require("@sanity/color"));
var import_color_fns = __toModule(require("../lib/color-fns"));
var import_theme = __toModule(require("../lib/theme"));
var import_helpers = __toModule(require("./helpers"));
const tones = {
  default: import_color.hues.gray,
  transparent: import_color.hues.gray,
  primary: import_color.hues.blue,
  positive: import_color.hues.green,
  caution: import_color.hues.yellow,
  critical: import_color.hues.red
};
const NEUTRAL_TONES = ["default", "transparent"];
const color = (0, import_theme.createColorTheme)({
  base: ({ dark, name }) => {
    if (name === "default") {
      const skeletonFrom2 = dark ? import_color.hues.gray[900].hex : import_color.hues.gray[100].hex;
      return {
        fg: dark ? import_color.white.hex : import_color.black.hex,
        bg: dark ? import_color.black.hex : import_color.white.hex,
        border: import_color.hues.gray[dark ? 800 : 200].hex,
        focusRing: import_color.hues.blue[dark ? 500 : 500].hex,
        shadow: {
          outline: (0, import_color_fns.rgba)(import_color.hues.gray[500].hex, 0.4),
          umbra: (0, import_color_fns.rgba)(dark ? import_color.black.hex : import_color.hues.gray[500].hex, 0.2),
          penumbra: (0, import_color_fns.rgba)(dark ? import_color.black.hex : import_color.hues.gray[500].hex, 0.14),
          ambient: (0, import_color_fns.rgba)(dark ? import_color.black.hex : import_color.hues.gray[500].hex, 0.12)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    if (name === "transparent") {
      const tints2 = tones.default;
      const skeletonFrom2 = tints2[dark ? 800 : 200].hex;
      return {
        fg: tints2[dark ? 100 : 900].hex,
        bg: tints2[dark ? 950 : 50].hex,
        border: tints2[dark ? 800 : 300].hex,
        focusRing: import_color.hues.blue[500].hex,
        shadow: {
          outline: (0, import_color_fns.rgba)(tints2[500].hex, dark ? 0.2 : 0.4),
          umbra: (0, import_color_fns.rgba)(dark ? import_color.black.hex : tints2[500].hex, 0.2),
          penumbra: (0, import_color_fns.rgba)(dark ? import_color.black.hex : tints2[500].hex, 0.14),
          ambient: (0, import_color_fns.rgba)(dark ? import_color.black.hex : tints2[500].hex, 0.12)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    const tints = tones[name] || tones.default;
    const skeletonFrom = tints[dark ? 800 : 200].hex;
    return {
      fg: tints[dark ? 100 : 900].hex,
      bg: tints[dark ? 950 : 50].hex,
      border: tints[dark ? 800 : 200].hex,
      focusRing: tints[500].hex,
      shadow: {
        outline: (0, import_color_fns.rgba)(tints[500].hex, dark ? 0.2 : 0.4),
        umbra: (0, import_color_fns.rgba)(dark ? import_color.black.hex : tints[500].hex, 0.2),
        penumbra: (0, import_color_fns.rgba)(dark ? import_color.black.hex : tints[500].hex, 0.14),
        ambient: (0, import_color_fns.rgba)(dark ? import_color.black.hex : tints[500].hex, 0.12)
      },
      skeleton: {
        from: skeletonFrom,
        to: (0, import_color_fns.rgba)(skeletonFrom, 0.5)
      }
    };
  },
  solid: ({ base, dark, name, state, tone }) => {
    const mix = dark ? import_helpers.screen : import_helpers.multiply;
    const mix2 = dark ? import_helpers.multiply : import_helpers.screen;
    const defaultTints = tones[name] || tones.default;
    const isNeutral = NEUTRAL_TONES.includes(name) && NEUTRAL_TONES.includes(tone);
    let tints = tones[tone === "default" ? name : tone] || defaultTints;
    if (state === "disabled") {
      tints = defaultTints;
      const bg2 = mix(base.bg, tints[dark ? 800 : 200].hex);
      const skeletonFrom2 = mix2(bg2, tints[dark ? 200 : 800].hex);
      return {
        bg: bg2,
        bg2: mix2(bg2, tints[dark ? 50 : 950].hex),
        border: mix(base.bg, tints[dark ? 800 : 200].hex),
        fg: mix(base.bg, dark ? import_color.black.hex : import_color.white.hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 950 : 50].hex)
        },
        accent: {
          fg: mix(base.bg, tints[dark ? 950 : 50].hex)
        },
        link: {
          fg: mix(base.bg, tints[dark ? 950 : 50].hex)
        },
        code: {
          bg: bg2,
          fg: mix(base.bg, tints[dark ? 950 : 50].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    if (state === "hovered") {
      const bg2 = mix(base.bg, tints[dark ? 300 : 600].hex);
      const skeletonFrom2 = mix2(bg2, tints[dark ? 200 : 800].hex);
      return {
        bg: bg2,
        bg2: mix2(bg2, tints[dark ? 50 : 950].hex),
        border: mix(base.bg, tints[dark ? 300 : 600].hex),
        fg: mix(base.bg, dark ? import_color.black.hex : import_color.white.hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 800 : 200].hex)
        },
        accent: {
          fg: mix2(bg2, import_color.hues.red[dark ? 800 : 200].hex)
        },
        link: {
          fg: mix2(bg2, import_color.hues.blue[dark ? 800 : 200].hex)
        },
        code: {
          bg: mix(bg2, tints[dark ? 950 : 50].hex),
          fg: mix(base.bg, tints[dark ? 800 : 200].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    if (state === "pressed") {
      const bg2 = mix(base.bg, tints[dark ? 200 : 800].hex);
      const skeletonFrom2 = mix2(bg2, tints[dark ? 200 : 800].hex);
      return {
        bg: mix(base.bg, tints[dark ? 200 : 800].hex),
        bg2: mix2(bg2, tints[dark ? 50 : 950].hex),
        border: mix(base.bg, tints[dark ? 200 : 800].hex),
        fg: mix(base.bg, dark ? import_color.black.hex : import_color.white.hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 800 : 200].hex)
        },
        accent: {
          fg: mix2(bg2, import_color.hues.red[dark ? 800 : 200].hex)
        },
        link: {
          fg: mix2(bg2, import_color.hues.blue[dark ? 800 : 200].hex)
        },
        code: {
          bg: mix(bg2, tints[dark ? 950 : 50].hex),
          fg: mix(base.bg, tints[dark ? 800 : 200].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    if (state === "selected") {
      if (isNeutral) {
        tints = tones.primary;
      }
      const bg2 = mix(base.bg, tints[dark ? 200 : 800].hex);
      const skeletonFrom2 = mix2(bg2, tints[dark ? 200 : 800].hex);
      return {
        bg: bg2,
        bg2: mix2(bg2, tints[dark ? 50 : 950].hex),
        border: mix(base.bg, tints[dark ? 200 : 800].hex),
        fg: mix(base.bg, dark ? import_color.black.hex : import_color.white.hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 800 : 200].hex)
        },
        accent: {
          fg: mix2(bg2, import_color.hues.red[dark ? 800 : 200].hex)
        },
        link: {
          fg: mix2(bg2, import_color.hues.blue[dark ? 800 : 200].hex)
        },
        code: {
          bg: mix(bg2, tints[dark ? 950 : 50].hex),
          fg: mix(base.bg, tints[dark ? 800 : 200].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    const bg = mix(base.bg, tints[dark ? 400 : 500].hex);
    const skeletonFrom = mix2(bg, tints[dark ? 200 : 800].hex);
    return {
      bg,
      bg2: mix2(bg, tints[dark ? 50 : 950].hex),
      border: mix(base.bg, tints[dark ? 400 : 500].hex),
      fg: mix(base.bg, dark ? import_color.black.hex : import_color.white.hex),
      muted: {
        fg: mix(base.bg, tints[dark ? 900 : 100].hex)
      },
      accent: {
        fg: mix2(bg, import_color.hues.red[dark ? 900 : 100].hex)
      },
      link: {
        fg: mix2(bg, import_color.hues.blue[dark ? 900 : 100].hex)
      },
      code: {
        bg: mix(bg, tints[dark ? 950 : 50].hex),
        fg: mix(base.bg, tints[dark ? 900 : 100].hex)
      },
      skeleton: {
        from: skeletonFrom,
        to: (0, import_color_fns.rgba)(skeletonFrom, 0.5)
      }
    };
  },
  muted: ({ base, dark, name, state, tone }) => {
    const mix = dark ? import_helpers.screen : import_helpers.multiply;
    const defaultTints = tones[name] || tones.default;
    const isNeutral = NEUTRAL_TONES.includes(name) && NEUTRAL_TONES.includes(tone);
    let tints = tones[tone === "default" ? name : tone] || defaultTints;
    if (state === "disabled") {
      tints = defaultTints;
      const bg2 = base.bg;
      const skeletonFrom2 = mix(bg2, tints[dark ? 900 : 100].hex);
      return {
        bg: bg2,
        bg2: mix(bg2, tints[dark ? 950 : 50].hex),
        border: mix(bg2, tints[dark ? 950 : 50].hex),
        fg: mix(bg2, tints[dark ? 800 : 200].hex),
        muted: {
          fg: mix(bg2, tints[dark ? 900 : 100].hex)
        },
        accent: {
          fg: mix(bg2, tints[dark ? 900 : 100].hex)
        },
        link: {
          fg: mix(bg2, tints[dark ? 900 : 100].hex)
        },
        code: {
          bg: bg2,
          fg: mix(bg2, tints[dark ? 900 : 100].hex)
        },
        skeleton: {
          from: (0, import_color_fns.rgba)(skeletonFrom2, 0.5),
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.25)
        }
      };
    }
    if (state === "hovered") {
      const bg2 = mix(base.bg, tints[dark ? 950 : 50].hex);
      const skeletonFrom2 = mix(bg2, tints[dark ? 900 : 100].hex);
      return {
        bg: bg2,
        bg2: mix(bg2, tints[dark ? 950 : 50].hex),
        border: mix(bg2, tints[dark ? 900 : 100].hex),
        fg: mix(base.bg, tints[dark ? 200 : 800].hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 400 : 600].hex)
        },
        accent: {
          fg: mix(base.bg, import_color.hues.red[dark ? 400 : 500].hex)
        },
        link: {
          fg: mix(base.bg, import_color.hues.blue[dark ? 400 : 600].hex)
        },
        code: {
          bg: mix(bg2, tints[dark ? 950 : 50].hex),
          fg: mix(base.bg, tints[dark ? 400 : 600].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    if (state === "pressed") {
      if (isNeutral) {
        tints = tones.primary;
      }
      const bg2 = mix(base.bg, tints[dark ? 900 : 100].hex);
      const skeletonFrom2 = mix(bg2, tints[dark ? 900 : 100].hex);
      return {
        bg: bg2,
        bg2: mix(bg2, tints[dark ? 950 : 50].hex),
        border: mix(bg2, tints[dark ? 900 : 100].hex),
        fg: mix(base.bg, tints[dark ? 200 : 800].hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 400 : 600].hex)
        },
        accent: {
          fg: mix(bg2, import_color.hues.red[dark ? 400 : 500].hex)
        },
        link: {
          fg: mix(bg2, import_color.hues.blue[dark ? 400 : 600].hex)
        },
        code: {
          bg: mix(bg2, tints[dark ? 950 : 50].hex),
          fg: mix(base.bg, tints[dark ? 400 : 600].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    if (state === "selected") {
      if (isNeutral) {
        tints = tones.primary;
      }
      const bg2 = mix(base.bg, tints[dark ? 900 : 100].hex);
      const skeletonFrom2 = mix(bg2, tints[dark ? 900 : 100].hex);
      return {
        bg: bg2,
        bg2: mix(bg2, tints[dark ? 950 : 50].hex),
        border: mix(bg2, tints[dark ? 900 : 100].hex),
        fg: mix(base.bg, tints[dark ? 200 : 800].hex),
        muted: {
          fg: mix(base.bg, tints[dark ? 400 : 600].hex)
        },
        accent: {
          fg: mix(bg2, import_color.hues.red[dark ? 400 : 500].hex)
        },
        link: {
          fg: mix(bg2, import_color.hues.blue[dark ? 400 : 600].hex)
        },
        code: {
          bg: mix(bg2, tints[dark ? 950 : 50].hex),
          fg: mix(base.bg, tints[dark ? 400 : 600].hex)
        },
        skeleton: {
          from: skeletonFrom2,
          to: (0, import_color_fns.rgba)(skeletonFrom2, 0.5)
        }
      };
    }
    const bg = base.bg;
    const skeletonFrom = mix(bg, tints[dark ? 900 : 100].hex);
    return {
      bg,
      bg2: mix(bg, tints[dark ? 950 : 50].hex),
      border: mix(bg, tints[dark ? 900 : 100].hex),
      fg: mix(base.bg, tints[dark ? 300 : 700].hex),
      muted: {
        fg: mix(base.bg, tints[dark ? 400 : 600].hex)
      },
      accent: {
        fg: mix(base.bg, import_color.hues.red[dark ? 400 : 500].hex)
      },
      link: {
        fg: mix(base.bg, import_color.hues.blue[dark ? 400 : 600].hex)
      },
      code: {
        bg: mix(base.bg, tints[dark ? 950 : 50].hex),
        fg: mix(base.bg, tints[dark ? 400 : 600].hex)
      },
      skeleton: {
        from: skeletonFrom,
        to: (0, import_color_fns.rgba)(skeletonFrom, 0.5)
      }
    };
  },
  button: ({ base, mode, muted, solid }) => {
    if (mode === "bleed") {
      return {
        enabled: __spreadProps(__spreadValues({}, muted.enabled), {
          border: muted.enabled.bg
        }),
        hovered: __spreadProps(__spreadValues({}, muted.hovered), {
          border: muted.hovered.bg
        }),
        pressed: __spreadProps(__spreadValues({}, muted.pressed), {
          border: muted.pressed.bg
        }),
        selected: __spreadProps(__spreadValues({}, muted.selected), {
          border: muted.selected.bg
        }),
        disabled: __spreadProps(__spreadValues({}, muted.disabled), {
          border: muted.disabled.bg
        })
      };
    }
    if (mode === "ghost") {
      return __spreadProps(__spreadValues({}, solid), {
        enabled: __spreadProps(__spreadValues({}, muted.enabled), {
          border: base.border
        }),
        disabled: muted.disabled
      });
    }
    return solid;
  },
  card: ({ base, dark, muted, name, solid, state }) => {
    if (state === "hovered") {
      return muted[name].hovered;
    }
    if (state === "disabled") {
      return muted[name].disabled;
    }
    const isNeutral = NEUTRAL_TONES.includes(name);
    const tints = tones[name] || tones.default;
    const mix = dark ? import_helpers.screen : import_helpers.multiply;
    if (state === "pressed") {
      if (isNeutral) {
        return muted.primary.pressed;
      }
      return muted[name].pressed;
    }
    if (state === "selected") {
      if (isNeutral) {
        return solid.primary.enabled;
      }
      return solid[name].enabled;
    }
    const bg = base.bg;
    const skeletonFrom = mix(base.bg, tints[dark ? 900 : 100].hex);
    return {
      bg,
      bg2: mix(bg, tints[dark ? 950 : 50].hex),
      fg: base.fg,
      border: base.border,
      muted: {
        fg: mix(base.bg, tints[dark ? 400 : 600].hex)
      },
      accent: {
        fg: mix(base.bg, import_color.hues.red[dark ? 400 : 500].hex)
      },
      link: {
        fg: mix(base.bg, import_color.hues.blue[dark ? 400 : 600].hex)
      },
      code: {
        bg: mix(base.bg, tints[dark ? 950 : 50].hex),
        fg: tints[dark ? 400 : 600].hex
      },
      skeleton: {
        from: skeletonFrom,
        to: (0, import_color_fns.rgba)(skeletonFrom, 0.5)
      }
    };
  },
  input: ({ base, dark, mode, state }) => {
    const mix = dark ? import_helpers.screen : import_helpers.multiply;
    if (mode === "invalid") {
      const tints = tones.critical;
      return {
        bg: mix(base.bg, tints[dark ? 950 : 50].hex),
        fg: mix(base.bg, tints[dark ? 400 : 600].hex),
        border: mix(base.bg, tints[dark ? 800 : 200].hex),
        placeholder: mix(base.bg, tints[dark ? 600 : 400].hex)
      };
    }
    if (state === "hovered") {
      return {
        bg: base.bg,
        fg: base.fg,
        border: mix(base.bg, import_color.hues.gray[dark ? 700 : 300].hex),
        placeholder: mix(base.bg, import_color.hues.gray[dark ? 600 : 400].hex)
      };
    }
    if (state === "disabled") {
      return {
        bg: mix(base.bg, import_color.hues.gray[dark ? 950 : 50].hex),
        fg: mix(base.bg, import_color.hues.gray[dark ? 700 : 300].hex),
        border: mix(base.bg, import_color.hues.gray[dark ? 900 : 100].hex),
        placeholder: mix(base.bg, import_color.hues.gray[dark ? 800 : 200].hex)
      };
    }
    if (state === "readOnly") {
      return {
        bg: mix(base.bg, import_color.hues.gray[dark ? 950 : 50].hex),
        fg: mix(base.bg, import_color.hues.gray[dark ? 200 : 800].hex),
        border: mix(base.bg, import_color.hues.gray[dark ? 800 : 200].hex),
        placeholder: mix(base.bg, import_color.hues.gray[dark ? 600 : 400].hex)
      };
    }
    return {
      bg: base.bg,
      fg: base.fg,
      border: base.border,
      placeholder: mix(base.bg, import_color.hues.gray[dark ? 600 : 400].hex)
    };
  },
  selectable: ({ base, muted, tone, solid, state }) => {
    if (state === "enabled") {
      return __spreadProps(__spreadValues({}, muted[tone].enabled), {
        bg: base.bg
      });
    }
    if (state === "pressed") {
      if (tone === "default") {
        return muted.primary.pressed;
      }
      return muted[tone].pressed;
    }
    if (state === "selected") {
      if (tone === "default") {
        return solid.primary.enabled;
      }
      return solid[tone].enabled;
    }
    if (state === "disabled") {
      return __spreadProps(__spreadValues({}, muted[tone].disabled), {
        bg: base.bg
      });
    }
    return muted[tone][state];
  },
  spot: ({ base, dark, key }) => {
    const mix = dark ? import_helpers.screen : import_helpers.multiply;
    return mix(base.bg, import_color.hues[key][dark ? 400 : 500].hex);
  },
  syntax: ({ base, dark }) => {
    const mix = dark ? import_helpers.screen : import_helpers.multiply;
    const mainShade = dark ? 400 : 600;
    const secondaryShade = dark ? 600 : 400;
    return {
      atrule: mix(base.bg, import_color.hues.purple[mainShade].hex),
      attrName: mix(base.bg, import_color.hues.green[mainShade].hex),
      attrValue: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      attribute: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      boolean: mix(base.bg, import_color.hues.purple[mainShade].hex),
      builtin: mix(base.bg, import_color.hues.purple[mainShade].hex),
      cdata: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      char: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      class: mix(base.bg, import_color.hues.orange[mainShade].hex),
      className: mix(base.bg, import_color.hues.cyan[mainShade].hex),
      comment: mix(base.bg, import_color.hues.gray[secondaryShade].hex),
      constant: mix(base.bg, import_color.hues.purple[mainShade].hex),
      deleted: mix(base.bg, import_color.hues.red[mainShade].hex),
      doctype: mix(base.bg, import_color.hues.gray[secondaryShade].hex),
      entity: mix(base.bg, import_color.hues.red[mainShade].hex),
      function: mix(base.bg, import_color.hues.green[mainShade].hex),
      hexcode: mix(base.bg, import_color.hues.blue[mainShade].hex),
      id: mix(base.bg, import_color.hues.purple[mainShade].hex),
      important: mix(base.bg, import_color.hues.purple[mainShade].hex),
      inserted: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      keyword: mix(base.bg, import_color.hues.magenta[mainShade].hex),
      number: mix(base.bg, import_color.hues.purple[mainShade].hex),
      operator: mix(base.bg, import_color.hues.magenta[mainShade].hex),
      prolog: mix(base.bg, import_color.hues.gray[secondaryShade].hex),
      property: mix(base.bg, import_color.hues.blue[mainShade].hex),
      pseudoClass: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      pseudoElement: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      punctuation: mix(base.bg, import_color.hues.gray[mainShade].hex),
      regex: mix(base.bg, import_color.hues.blue[mainShade].hex),
      selector: mix(base.bg, import_color.hues.red[mainShade].hex),
      string: mix(base.bg, import_color.hues.yellow[mainShade].hex),
      symbol: mix(base.bg, import_color.hues.purple[mainShade].hex),
      tag: mix(base.bg, import_color.hues.red[mainShade].hex),
      unit: mix(base.bg, import_color.hues.orange[mainShade].hex),
      url: mix(base.bg, import_color.hues.red[mainShade].hex),
      variable: mix(base.bg, import_color.hues.red[mainShade].hex)
    };
  }
});
