import { createContext } from "react";
import { globalScope } from "../../lib/globalScope";
const key = Symbol.for("@sanity/ui/context/menu");
globalScope[key] = globalScope[key] || createContext(null);
const MenuContext = globalScope[key];
export {
  MenuContext
};
