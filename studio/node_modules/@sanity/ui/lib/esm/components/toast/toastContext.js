import { createContext } from "react";
import { globalScope } from "../../lib/globalScope";
const key = Symbol.for("@sanity/ui/context/toast");
globalScope[key] = globalScope[key] || createContext(null);
const ToastContext = globalScope[key];
export {
  ToastContext
};
