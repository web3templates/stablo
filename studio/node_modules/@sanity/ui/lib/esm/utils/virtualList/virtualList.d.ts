import React from 'react';
import { StackProps } from '../../primitives';
/**
 * @beta
 */
export interface VirtualListChangeOpts {
    fromIndex: number;
    gap: number;
    itemHeight: number;
    scrollHeight: number;
    scrollTop: number;
    toIndex: number;
}
/**
 * @beta
 */
export interface VirtualListProps<Item = any> {
    as?: React.ElementType | keyof JSX.IntrinsicElements;
    gap?: number;
    getItemKey?: (item: Item, itemIndex: number) => string;
    items?: Item[];
    onChange?: (opts: VirtualListChangeOpts) => void;
    renderItem?: (item: Item) => React.ReactNode;
}
/**
 * @beta
 */
export declare const VirtualList: React.ForwardRefExoticComponent<VirtualListProps<any> & StackProps & Omit<React.HTMLProps<HTMLDivElement>, "children" | "ref" | "onChange" | "as"> & React.RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=virtualList.d.ts.map