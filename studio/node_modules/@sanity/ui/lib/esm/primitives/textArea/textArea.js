var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __require = typeof require !== "undefined" ? require : (x) => {
  throw new Error('Dynamic require of "' + x + '" is not supported');
};
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
import React, { forwardRef } from "react";
import styled from "styled-components";
import { useForwardedRef, useCustomValidity } from "../../hooks";
import {
  responsiveInputPaddingStyle,
  responsiveRadiusStyle,
  textInputRootStyle,
  textInputBaseStyle,
  textInputFontSizeStyle,
  textInputRepresentationStyle
} from "../../styles/internal";
const Root = styled.span(textInputRootStyle);
const InputRoot = styled.span`
  flex: 1;
  min-width: 0;
  display: block;
  position: relative;
`;
const Input = styled.textarea(responsiveInputPaddingStyle, textInputBaseStyle, textInputFontSizeStyle);
const Presentation = styled.div(responsiveRadiusStyle, textInputRepresentationStyle);
const TextArea = forwardRef(function TextArea2(props, forwardedRef) {
  const _a = props, {
    border = true,
    customValidity,
    disabled = false,
    fontSize = 2,
    padding = 3,
    radius = 1
  } = _a, restProps = __objRest(_a, [
    "border",
    "customValidity",
    "disabled",
    "fontSize",
    "padding",
    "radius"
  ]);
  const ref = useForwardedRef(forwardedRef);
  useCustomValidity(ref, customValidity);
  return /* @__PURE__ */ React.createElement(Root, {
    "data-ui": "TextArea"
  }, /* @__PURE__ */ React.createElement(InputRoot, null, /* @__PURE__ */ React.createElement(Input, __spreadProps(__spreadValues({
    "data-as": "textarea"
  }, restProps), {
    $fontSize: fontSize,
    $padding: padding,
    disabled,
    ref
  })), /* @__PURE__ */ React.createElement(Presentation, {
    $border: border,
    $radius: radius
  })));
});
export {
  TextArea
};
