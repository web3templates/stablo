"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getDupes = getDupes;

function getDupes(array) {
  var selector = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : v => v;
  var dupes = array.reduce((acc, item) => {
    var key = selector(item);

    if (!acc[key]) {
      acc[key] = [];
    }

    acc[key].push(item);
    return acc;
  }, {});
  return Object.keys(dupes).map(key => dupes[key].length > 1 ? dupes[key] : null).filter(Boolean);
}