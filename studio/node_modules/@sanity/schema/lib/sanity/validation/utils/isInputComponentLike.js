"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isInputComponentLike = isInputComponentLike;
var REACT_SYM_RE = /^Symbol\(react\..+\)$/;

function isInputComponentLike(value) {
  var type = typeof value; // Note: we're not using `isValidElementType` from react-is here since it accepts too much, e.g. any strings.

  return type === 'function' || typeof (value === null || value === void 0 ? void 0 : value.$$typeof) === 'symbol' && REACT_SYM_RE.test(String(value === null || value === void 0 ? void 0 : value.$$typeof));
}