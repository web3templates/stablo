"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.format = format;
exports.parse = parse;

var _moment = _interopRequireDefault(require("moment"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* eslint-disable no-shadow */
// todo: find a way to get rid of moment there.
// note: the format comes form peoples schemas, so we need to deprecate it for a while and
// find a way to tell people that they need to change it
function format(input, format) {
  return (0, _moment.default)(input).format(format);
}

function parse(dateString, format) {
  var parsed = (0, _moment.default)(dateString, format, true);

  if (parsed.isValid()) {
    return {
      isValid: true,
      date: parsed.toDate()
    };
  }

  return {
    isValid: false,
    error: "Invalid date. Must be on the format \"".concat(format, "\"")
  };
}