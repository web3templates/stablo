export declare type ParseResult = {
    isValid: boolean;
    date?: Date;
    error?: string;
} & ({
    isValid: true;
    date: Date;
} | {
    isValid: false;
    error?: string;
});
export declare function format(input: Date, format: string): string;
export declare function parse(dateString: string, format: string): ParseResult;
//# sourceMappingURL=legacyDateFormat.d.ts.map