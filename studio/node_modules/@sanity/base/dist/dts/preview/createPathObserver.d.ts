import { Observable } from 'rxjs';
import { FieldName, Path, Value } from './types';
declare type ObserveFieldsFn = (id: string, fields: FieldName[]) => Observable<Record<string, any> | null>;
export declare function createPathObserver(observeFields: ObserveFieldsFn): (value: Value, paths: (FieldName | Path)[]) => Observable<Record<string, unknown> | null>;
export {};
//# sourceMappingURL=createPathObserver.d.ts.map