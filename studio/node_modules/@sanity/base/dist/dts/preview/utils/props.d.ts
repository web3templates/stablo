import { Observable } from 'rxjs';
declare type Props<K extends keyof any, T> = {
    [P in K]: T | Observable<T>;
};
export default function props<K extends keyof any, T>(options?: {
    wait?: boolean;
}): (source: Observable<Props<K, T>>) => Observable<{}>;
export {};
//# sourceMappingURL=props.d.ts.map