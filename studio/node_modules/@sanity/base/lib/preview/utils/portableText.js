"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isPortableTextArray = isPortableTextArray;
exports.extractTextFromBlocks = extractTextFromBlocks;

function isBlock(block) {
  return block && Array.isArray(block.markDefs) && Array.isArray(block.children);
}

function isSpan(span) {
  return span && span._type === 'span' && typeof span.text === 'string';
}

function isPortableTextArray(blocks) {
  return Array.isArray(blocks) && (blocks.length === 0 || blocks.some(isBlock));
}

function extractTextFromBlocks(blocks) {
  var firstBlock = blocks.find(isBlock);

  if (!firstBlock || !firstBlock.children) {
    return '';
  }

  return firstBlock.children.filter(isSpan).map(span => span.text).join('');
}