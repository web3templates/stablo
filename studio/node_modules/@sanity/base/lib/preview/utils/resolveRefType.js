"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = resolveRefType;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _versionedClient = require("../../client/versionedClient");

var _constants = require("../constants");

// todo: use a LRU cache instead (e.g. hashlru or quick-lru)
var CACHE = Object.create(null);

function resolveRefTypeName(reference) {
  if (!(reference._ref in CACHE)) {
    CACHE[reference._ref] = _versionedClient.versionedClient.fetch('*[_id == $id][0]._type', {
      id: reference._ref
    }, {
      tag: 'preview.resolve-ref-type'
    });
  }

  return (0, _rxjs.from)(CACHE[reference._ref]);
}

function resolveRefType(value, type) {
  return resolveRefTypeName(value).pipe((0, _operators.map)(refTypeName => {
    if (!refTypeName && !type.weak) {
      throw new _constants.InsufficientPermissionsError("Could not resolve _type ".concat(refTypeName, " and the reference was marked as weak."));
    }

    return type.to.find(toType => toType.name === refTypeName);
  }));
}