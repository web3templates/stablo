"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resolveBackend = void 0;

var _localStorage = _interopRequireDefault(require("./localStorage"));

var _memory = _interopRequireDefault(require("./memory"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var isSupported = null;

var supportsLocalStorage = () => {
  if (isSupported !== null) {
    return isSupported;
  }

  var testKey = '__test__';

  try {
    localStorage.setItem(testKey, testKey);
    localStorage.removeItem(testKey);
    isSupported = true;
  } catch (evt) {
    isSupported = false;
  }

  return isSupported;
};

var resolveBackend = () => supportsLocalStorage() ? _localStorage.default : _memory.default;

exports.resolveBackend = resolveBackend;