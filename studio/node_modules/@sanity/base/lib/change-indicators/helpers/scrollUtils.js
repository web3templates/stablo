"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hasOverflowScroll = hasOverflowScroll;
exports.isScrollable = isScrollable;

function hasOverflowScroll(el) {
  var overflow = getComputedStyle(el).overflow;
  return overflow.includes('auto') || overflow.includes('scroll');
} // Check whether an element is currently scrollable (meaning it must both have overflow scroll *and* content overflowing)


function isScrollable(el) {
  var scrollableContent = el.scrollHeight !== el.offsetHeight || el.scrollWidth !== el.offsetWidth;
  return scrollableContent && hasOverflowScroll(el);
}