"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.markersToValidationList = markersToValidationList;

var _types = require("@sanity/types");

function markersToValidationList(markers) {
  var validationMarkers = markers.filter(_types.isValidationMarker);
  return validationMarkers.map(marker => {
    return {
      type: marker.level === 'error' ? 'error' : 'warning',
      label: marker.item.message
    };
  });
}