"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getPlaceholderItemStyles;

var _range2 = _interopRequireDefault(require("lodash/range"));

var _circularAt = _interopRequireDefault(require("circular-at"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var POOL_SIZE = 50;
var pool = (0, _range2.default)(POOL_SIZE).map(createItemStyles);

function createItemStyles() {
  return {
    date: {
      width: "".concat(Math.random() * 10 + 15, "%")
    },
    title: {
      width: "".concat(Math.random() * 80 + 15, "%")
    },
    subtitle: {
      width: "".concat(Math.random() * 80 + 15, "%")
    },
    description: {
      width: "".concat(Math.random() * 80 + 15, "%")
    }
  };
}

function getPlaceholderItemStyles(index) {
  return (0, _circularAt.default)(pool, index);
}