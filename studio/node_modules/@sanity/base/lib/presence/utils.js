"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.split = exports.splitRight = void 0;

var splitRight = (array, max) => {
  var indexFromMax = array.length > max ? max - 1 : max;
  var idx = Math.max(0, array.length - indexFromMax);
  return [array.slice(0, idx), array.slice(idx)];
}; // export const splitRight = <T>(array: T[], index: number): [T[], T[]] => {
//   const idx = Math.max(0, array.length - index)
//   return [array.slice(0, idx), array.slice(idx)]
// }
//


exports.splitRight = splitRight;

var split = (array, index) => {
  var idx = Math.max(0, index);
  return [array.slice(0, idx), array.slice(idx)];
};

exports.split = split;