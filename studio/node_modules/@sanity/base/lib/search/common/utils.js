"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSearchableTypes = void 0;

var isDocumentType = type => type.type && type.type.name === 'document';

var isSanityType = type => type.name.startsWith('sanity.');

var getSearchableTypes = schema => schema.getTypeNames().map(typeName => schema.get(typeName)).filter(isDocumentType).filter(type => !isSanityType(type));

exports.getSearchableTypes = getSearchableTypes;