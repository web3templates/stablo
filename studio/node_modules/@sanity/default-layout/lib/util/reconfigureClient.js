"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = reconfigureClient;

var _client = _interopRequireDefault(require("part:@sanity/base/client"));

var _spaces = require("./spaces");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// @todo: remove the following line when part imports has been removed from this file
///<reference types="@sanity/types/parts" />
function reconfigureClient(routerState) {
  var space = _spaces.CONFIGURED_SPACES.find(sp => sp.name === routerState.space);

  if (space && space.api) {
    ;

    _client.default.config(space.api, true);
  }
}