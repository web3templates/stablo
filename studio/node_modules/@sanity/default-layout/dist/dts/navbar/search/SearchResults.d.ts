import React from 'react';
import { ResultItem } from './types';
interface Props {
    activeIndex: number;
    error?: {
        message: string;
    };
    isBleeding: boolean;
    isLoading: boolean;
    items: ResultItem[];
    query: string;
    renderItem: (item: ResultItem, index: number, className: string) => React.ReactNode;
}
declare class SearchResults extends React.PureComponent<Props> {
    static defaultProps: {
        error: any;
    };
    element: HTMLElement | null;
    setElement: (ref: HTMLElement) => void;
    render(): JSX.Element;
}
export default SearchResults;
//# sourceMappingURL=SearchResults.d.ts.map