/**
 *
 * This plugin will change Slate's default marks model (every prop is a mark) with the Portable Text model (marks is an array of strings on prop .marks).
 *
 */
import { Subject } from 'rxjs';
import { EditorChange, PortableTextSlateEditor } from '../../types/editor';
import { PortableTextFeatures } from '../../types/portableText';
export declare function createWithPortableTextMarkModel(portableTextFeatures: PortableTextFeatures, keyGenerator: () => string, change$: Subject<EditorChange>): (editor: PortableTextSlateEditor) => PortableTextSlateEditor;
//# sourceMappingURL=createWithPortableTextMarkModel.d.ts.map