import { ReactEditor } from '@sanity/slate-react';
import { PortableTextFeatures } from '../types/portableText';
/**
 * Check if the target is editable and in the editor.
 */
export declare const hasEditableTarget: (editor: ReactEditor, target: EventTarget | null) => target is Node;
/**
 * Set the currently selected Portable Text fragment to the clipboard.
 */
export declare const setFragmentData: (dataTransfer: DataTransfer, editor: ReactEditor, portableTextFeatures: PortableTextFeatures) => void;
//# sourceMappingURL=copyPaste.d.ts.map