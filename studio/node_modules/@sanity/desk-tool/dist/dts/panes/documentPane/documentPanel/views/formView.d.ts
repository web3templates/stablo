import React from 'react';
import { Subscription } from 'rxjs';
import { Path } from '@sanity/types';
import { Doc } from '../../types';
interface Props {
    id: string;
    readOnly?: boolean;
    value: Doc | null;
    compareValue: Doc | null;
    initialValue: Doc;
    isConnected: boolean;
    onChange: (patches: any[]) => void;
    schemaType: {
        name: string;
        title: string;
    };
    markers: Array<{
        path: any[];
    }>;
    focusPath: Path;
    onFocus: (path: Path) => void;
    margins: [number, number, number, number];
}
export declare class FormView extends React.PureComponent<Props> {
    static defaultProps: {
        markers: never[];
        isConnected: boolean;
    };
    state: {
        filterField: () => boolean;
    };
    filterFieldFnSubscription: Subscription | null;
    componentDidMount(): void;
    componentWillUnmount(): void;
    handleBlur: () => void;
    handleEditAsActualType: () => void;
    isReadOnly(): boolean;
    render(): JSX.Element;
}
export {};
//# sourceMappingURL=formView.d.ts.map