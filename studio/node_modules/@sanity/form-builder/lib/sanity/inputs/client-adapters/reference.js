"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPreviewSnapshot = getPreviewSnapshot;
exports.search = search;

var _operators = require("rxjs/operators");

var _legacyParts = require("../../../legacyParts");

var _versionedClient = require("../../versionedClient");

function getPreviewSnapshot(value, referenceType) {
  return (0, _legacyParts.observeForPreview)(value, referenceType).pipe((0, _operators.map)(result => result.snapshot));
}

function search(textTerm, type, options) {
  var searchWeighted = (0, _legacyParts.createWeightedSearch)(type.to, _versionedClient.searchClient, options);
  return searchWeighted(textTerm, {
    includeDrafts: false
  }).pipe((0, _operators.map)(results => results.map(_ref => {
    var hit = _ref.hit;
    return {
      _type: hit._type,
      _id: hit._id
    };
  })));
}