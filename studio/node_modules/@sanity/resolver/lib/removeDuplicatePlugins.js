"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = removeDuplicatePlugins;

function removeDuplicatePlugins(plugins) {
  var paths = [];
  return plugins.filter(plugin => {
    if (paths.indexOf(plugin.path) !== -1) {
      return false;
    }

    paths.push(plugin.path);
    return true;
  });
}