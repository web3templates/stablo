"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSizeDiff = getSizeDiff;

function getSizeDiff(prev, next) {
  if (!prev || !next) {
    return 0;
  }

  var increase = next - prev;
  var pct = Math.round(increase / prev * 100);
  return pct;
}