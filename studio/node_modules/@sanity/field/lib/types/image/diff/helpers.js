"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hexToRgba = hexToRgba;
exports.simpleHash = simpleHash;
exports.getDeviceDpr = getDeviceDpr;

// @todo: use `polished` for this?
function hexToRgba(hex, opacity) {
  var rgba = (/^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex) || []).slice(1).map(num => parseInt(num, 16)).concat(opacity);
  return "rgba(".concat(rgba.join(', '), ")");
} // @todo: replace this


function simpleHash(str) {
  var hash = 0;

  if (str.length == 0) {
    return hash.toString();
  }

  for (var i = 0; i < str.length; i++) {
    var char = str.charCodeAt(i); // eslint-disable-next-line no-bitwise

    hash = (hash << 5) - hash + char; // eslint-disable-next-line no-bitwise

    hash &= hash; // Convert to 32bit integer
  }

  return hash.toString();
}

function getDeviceDpr() {
  var base = Math.ceil(window.devicePixelRatio || 1);
  return Math.min(3, Math.max(1, base));
}