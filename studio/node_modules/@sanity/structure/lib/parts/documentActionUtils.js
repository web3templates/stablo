"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.resolveEnabledActions = exports.isActionEnabled = void 0;

// We are lazy-loading the part to work around typescript trying to resolve it
var isActionEnabled = (() => {
  var documentActionUtils = require('part:@sanity/base/util/document-action-utils');

  return (type, action) => type ? documentActionUtils.isActionEnabled(type, action) : false;
})();

exports.isActionEnabled = isActionEnabled;

var resolveEnabledActions = (() => {
  var documentActionUtils = require('part:@sanity/base/util/document-action-utils');

  return type => documentActionUtils.resolveEnabledActions(type);
})();

exports.resolveEnabledActions = resolveEnabledActions;