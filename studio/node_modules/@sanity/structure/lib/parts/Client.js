"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.structureClient = exports.client = void 0;

var _getDefaultModule = _interopRequireDefault(require("./getDefaultModule"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Default client for queries, using API version 1 for backwards compatibility
 *
 * @internal
 */
var client = (() => {
  // We are lazy-loading the part to work around typescript trying to resolve it
  var sanityClient = (0, _getDefaultModule.default)(require('part:@sanity/base/client'));
  return sanityClient.withConfig({
    apiVersion: '1'
  });
})();
/**
 * For structure-internal requests that we have control of the filter on,
 * we'll use this client with a more modern API version
 *
 * @internal
 */


exports.client = client;
var structureClient = client.withConfig({
  apiVersion: '2021-06-07'
});
exports.structureClient = structureClient;