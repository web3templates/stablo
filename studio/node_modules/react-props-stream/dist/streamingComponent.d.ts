import * as React from 'react';
import { Observable } from 'rxjs';
declare type ObservableFactory<SourceProps> = ((props$: Observable<SourceProps>) => Observable<React.ReactNode>);
export declare function streamingComponent<SourceProps>(observableOrFactory: Observable<SourceProps> | ObservableFactory<SourceProps>): React.ComponentType<SourceProps>;
export {};
