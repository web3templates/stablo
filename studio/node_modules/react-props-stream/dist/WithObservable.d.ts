import * as React from 'react';
import { Observable } from 'rxjs';
interface Props<T> {
    observable: Observable<T>;
    children?: (value: T) => React.ReactElement<any>;
}
export declare const WithObservable: React.ComponentType<Props<unknown>>;
export {};
